


const defaultValue = {
    movies:[],
    series:[],
    error:null,
    loading:null

}

const reducer =(state= defaultValue,action)=>{
    switch(action.type) {
        
        
        default:{
            return state} 
    }
}
export default reducer